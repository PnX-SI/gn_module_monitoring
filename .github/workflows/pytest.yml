name: pytest

on:
  push:
    paths:
      - "backend/**"
    branches:
      - main
      - hotfixes
      - develop 
  pull_request:
    paths:
      - "backend/**"
    branches:
      - main
      - hotfixes
      - develop

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        geonature_ref: ["master"] # Mettre version compatible
    uses: pnx-si/geonature/.github/workflows/gn-module-pytest.yml@fix/submodule_code_cov
    with:
      geonature_ref: ${{ matrix.geonature_ref }}
      upload_coverage: true
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
